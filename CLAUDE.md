# Development notes
- Do not commit/push on your own.
- Do not launch/run the game (emulator) on your own to test changes — let the human developer run and test it themselves. Verify via build success, code reading, and (if truly needed) narrow temp-debug instrumentation instead.
- In special cases where running the emulator really is necessary to verify something, ask for permission first and explain why, rather than just doing it.
